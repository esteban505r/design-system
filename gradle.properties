org.gradle.jvmargs=-Xmx2g -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true

# Maven coordinates for GitHub Packages (used in consumer: implementation("groupId:artifactId:version"))
mavenGroupId=com.estebanruano
mavenArtifactId=tokens-android

# Android library namespace (R class / manifest package). Use a reverse-DNS name you control.
tokensAndroidNamespace=com.estebanruano.tokens
